# smacedu-homepage

스마트미디어아트센터(엘란비탈) 홈페이지 저장소.
정적 HTML 페이지 + Supabase + Vercel 구성. 강의 랜딩페이지가 주력 산출물.

---

## 🧠 이 저장소는 "브랜드 기억장치"입니다

세션은 끝나면 사라지고, 계정이 다르면 서로의 기록을 볼 수 없습니다.
그래서 **배운 것은 반드시 저장소에 남깁니다.** 저장소에 없는 지식은 존재하지 않는 지식입니다.

지식은 `brand/` 에 쌓입니다. 진입점은 **[brand/INDEX.md](brand/INDEX.md)** 입니다.

### 세션 시작 프로토콜 (필수)

브랜딩·카피·디자인·랜딩페이지와 관련된 작업을 시작하기 전에 **반드시 먼저 읽습니다**:

1. `brand/INDEX.md` — 전체 지도
2. `brand/LOG.md` 최근 5개 항목 — 직전 세션들이 무엇을 했고 무엇이 미해결인지
3. 작업과 관련된 개별 문서 (`voice.md` / `design.md` / `strategy.md` / `sources/`)

읽지 않고 시작하면 이미 정해진 톤과 전략을 무시한 결과물이 나옵니다.

### 세션 종료 프로토콜 (필수)

작업이 끝나면, 그 세션에서 **새로 알게 된 것이 하나라도 있으면** 반드시:

1. 해당 지식을 `brand/` 의 알맞은 문서에 반영 (새 파일보다 **기존 문서 갱신**을 우선)
2. `brand/LOG.md` 맨 위에 한 항목 추가 (날짜 / 한 일 / 배운 것 / 다음 할 일)
3. 커밋 + 푸시

"문서함에 넣는 것"이 목적이 아닙니다. **다음 세션이 더 똑똑해지는 것**이 목적입니다.
그러므로 원문을 그대로 붙여넣지 말고, **재사용 가능한 규칙과 판단 근거로 요약**해서 적습니다.

---

## 📥 외부 자료를 가져오는 법

클라우드 세션(claude.ai/code, 모바일, 웹)은 다음에 접근할 수 없습니다:

- 로컬/네트워크 드라이브 (`W:\...`, `file:///...`)
- 네이버 (`cafe.naver.com`, `blog.naver.com`) — 조직 네트워크 정책으로 차단됨
- 로그인이 필요한 모든 페이지

접근이 안 되는데도 "된다"고 가정하고 내용을 지어내면 안 됩니다. 대신:

| 상황 | 해결 방법 |
|---|---|
| 로컬 PC에 있는 파일 | 로컬 Claude(Desktop/VS Code) 세션에서 `brand/inbox/` 로 복사 후 커밋 |
| 네이버 카페/블로그 글 | 본문 복사 → `brand/inbox/` 에 `.md` 로 저장, 또는 대화창에 붙여넣기 |
| 디자인 참고 화면 | 스크린샷을 `brand/inbox/` 에 이미지로 저장 |

`brand/inbox/` 에 들어온 원본은 분석 후 `brand/sources/` 에 **분석 결과**로 정리하고,
거기서 뽑아낸 규칙은 `voice.md` / `design.md` / `strategy.md` 에 반영합니다.

---

## ✍️ 결과물을 만들 때

랜딩페이지·카피·이메일·카드뉴스를 만들 때는 `brand/voice.md` 와 `brand/design.md` 를
"참고"가 아니라 **기준**으로 씁니다. 기존 페이지의 실제 구현은
`documents/랜딩페이지_제작_가이드.md` 와 `documents/landing_page_10_core_strategies.md` 를 함께 봅니다.

---

## 🔧 기술 메모

- 정적 HTML. 빌드 단계 없음. 페이지를 열면 그대로 동작.
- 백엔드: Supabase (`supabase-schema.sql`, `js/` 안의 클라이언트 코드)
- 폼 수집 일부는 Google Apps Script (`*-apps-script.gs`)
- 배포: Vercel (`vercel.json`)
- 비밀값은 `.env.example` 참고. **실제 키를 커밋하지 않습니다.**
